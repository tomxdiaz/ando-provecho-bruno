info:
  name: get all restaurants
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/restaurant"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
