info:
  name: get restaurant by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/restaurant/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
