info:
  name: sign-in
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/auth/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "tomydiaz739@gmail.com",
        "password": "zedcampeonato1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
