info:
  name: ver categorias
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/restaurant/1/menu/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
