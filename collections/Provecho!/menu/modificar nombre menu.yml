info:
  name: modificar nombre menu
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/restaurant/1/menu"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
